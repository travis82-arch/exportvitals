{
  "name": "exportvitals",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check:nav": "node scripts/check-nav-manifest.mjs",
    "build": "vite build",
    "check:mpa": "node scripts/check-nav-manifest.mjs && node scripts/check-no-jsx-imports.mjs"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "papaparse": "^5.4.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}

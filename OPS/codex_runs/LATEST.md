# Latest Run - 2026-03-04

## Root causes (file + line)
- `src/mpa-entry.js` (around old selected-date init block, now at lines 59-61): preferred date fallback logic was wrong for `resolveInitialSelectedDate` flow; it should pass `(dates, preferred)` with `preferred` falling back to `null`.
- `src/mpa-entry.js` (old top-of-boot position; now moved to line 197): `document.documentElement.dataset.js = '1'` was set before render success, hiding `#bootShell` even on boot failure.
- `src/mpa-entry.js` + `src/components/TopNav.js` (now lines 72 and 12): import trigger wiring relied on button flow and did not wire a top-nav file input change path to `openWithFile(file)`.
- `src/style.css` lines 46-50: modal visibility/layering styles were too weak/inconsistent for reliable modal presentation.

## Files changed
- `src/mpa-entry.js`
- `src/components/TopNav.js`
- `src/style.css`
- `dist/*` regenerated by `npm run build`

## Build result
- Command: `npm run build`
- Result: PASS (`vite build` completed successfully)

## Manual verification checklist
- [x] Opened app via Vite preview server (`npx vite preview --host 127.0.0.1 --port 4173 --strictPort`).
- [x] Route pages resolve with expected titles:
  - `index.html` -> `Oura Dashboard · By Date`
  - `sleep.html` -> `Oura Dashboard · Sleep`
  - `readiness.html` -> `Oura Dashboard · Readiness`
  - `data-tools-import.html` -> `Oura Dashboard · Import`
- [x] Tab navigation remains anchor-based (`<a href="...">`) in `src/components/TopNav.js`.
- [x] Import page button `#openImport` calls `importController.open()` in `src/mpa-entry.js`.
- [x] Top-nav import input `#globalImportInput` change handler calls `importController.openWithFile(file)` in `src/mpa-entry.js`.
- [ ] Full interactive browser-only checks (physically selecting a ZIP and visually confirming progress/no fatal overlay) are not fully reproducible from this non-interactive terminal session.

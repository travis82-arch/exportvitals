# Latest Run - 2026-03-08

## Root causes for dead clicks/import
- Top-nav import input used `hidden`, which can prevent reliable mobile picker invocation when triggered via label; switching to off-screen but present input fixes this path.
- Old service worker + cache artifacts could pin stale assets, causing no-op clicks and outdated JS behavior after deploy; startup now purges stale SW/caches and performs one cache-busted reload.
- Several primary pages (`sleep`, `journal`, `settings`, `debug`) were falling through to generic placeholder content, creating the appearance of dead/incomplete UI after navigation.
- Baseline logic for score metrics used direct metric keys against wrong row fields (`sleepScore/readinessScore/activityScore`), producing invalid baselines.

## Files changed
- `src/boot/swPurge.js` (new)
- `src/state/settings.js` (new)
- `src/components/TopNav.js`
- `src/store/dataStore.js`
- `src/mpa-entry.js`
- `src/style.css`
- `src/mappings/byDateUiMapping.js`
- `src/mappings/sleepUiMapping.js`
- `src/mappings/journalUiMapping.js` (new)
- `src/mappings/importUiMapping.js` (new)
- `src/mappings/exportUiMapping.js` (new)
- `src/mappings/settingsUiMapping.js` (new)
- `src/mappings/debugUiMapping.js` (new)
- `sw.js`
- HTML templates (CDN script removal): `index.html`, `sleep.html`, `readiness.html`, `activity.html`, `vitals.html`, `trends.html`, `journal.html`, `data-tools-import.html`, `data-tools-export.html`, `glossary.html`, `settings.html`, `debug.html`, `my-health.html`
- `dist/*` regenerated by `npm run build` (including `dist/_redirects` and `dist/_headers` present)

## Commands run + results
1. `npm run build`
- First attempt: FAIL
- Error: top-level `await` in `src/mpa-entry.js` not supported for configured target.
- Fix applied: wrapped startup in async `bootstrap()`.
- Second attempt: PASS (vite build completed).

2. `npm run check:mpa`
- PASS
- Output: `MPA nav manifest guard passed.` and `check-no-jsx-imports passed.`

3. `node scripts/smoke-import-local.mjs` (conditional)
- Skipped because `OPS/_local/data.zip` is missing.

4. Dist artifact check
- PASS: `dist/_redirects` and `dist/_headers` exist after build.

5. Required `data-page` check (source HTML)
- PASS for all required pages:
  - `index`, `sleep`, `readiness`, `activity`, `vitals`, `trends`, `journal`, `data-tools-import`, `data-tools-export`, `glossary`, `settings`, `debug`.

## Smoke import output
- `skipped: OPS/_local/data.zip missing`
- `rowCounts/daysPerDataset`: not available because local zip is absent.

## Manual verification checklist + results
- [ ] Open app via `npx vite preview --host`/`npx vite` and click through routes.
- [ ] Verify top-right import icon opens file picker on desktop/mobile.
- [ ] Import ZIP and confirm progress + success banner + reload.
- [ ] Verify Sleep page HR/HRV rendering (or explicit placeholders when missing).
- [ ] Verify Settings `Clear app cache` performs reset + reload.

Notes:
- I attempted to automate a local preview + route fetch flow from terminal, but background server/process orchestration was blocked by runtime policy in this environment.
- Interactive browser actions (real clicking/file-picker invocation) require a local GUI session, so these items remain unchecked here.
